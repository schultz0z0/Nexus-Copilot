services:
  artifact-server:
    ports:
      - "${ARTIFACT_PORT:-8095}:8095"

  chat-bridge:
    ports:
      - "${BRIDGE_PORT:-8080}:8080"

  app-api:
    ports:
      - "${APP_API_PORT:-3000}:3000"

  chat-web:
    ports:
      - "${CHAT_WEB_PORT:-8088}:8080"
